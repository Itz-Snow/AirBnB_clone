from setuptools import setup, find_packages

setup(
    name='AirBnB_clone_demo',
    version='1.0',
    packages=find_packages(),
    install_requires=[
        # Any dependencies the project may have
    ],
)
